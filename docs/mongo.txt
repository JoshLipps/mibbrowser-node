Mongo "schema".txt


mb.events: {
    seqnumber: key,
    device:    mb.devices.hostname,
    alarmname: mb.devices.alarms.oid,
    state,
    description,
    datestamp
}

mb.devices: {
    hostname: key,
    community,
    alarms: [
        {
            oid,
            error,
            warn,
            clear
        }
    ]
}